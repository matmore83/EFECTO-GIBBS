[project]
name = "tp-final"
version = "0.1.0"
description = "Trabajo final"
requires-python = ">=3.10.4"

dependencies = [
]

[dependency-groups]
dev = [
    "matplotlib>=3.10.9",
    "numpy>=2.2.6",
    "pyqt6>=6.11.0",
    "pyqtgraph>=0.14.0",
    "scipy>=1.15.3",
]

[tool.setuptools.packages.find]
where = ["."]